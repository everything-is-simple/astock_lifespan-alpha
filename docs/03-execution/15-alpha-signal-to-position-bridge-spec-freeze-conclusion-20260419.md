# 阶段四批次 15 alpha_signal -> position 桥接规格冻结结论

结论编号：`15`
日期：`2026-04-19`
状态：`已接受`

## 1. 裁决

- 接受：阶段四 `alpha_signal -> position` 的唯一正式桥接合同已经冻结。
- 拒绝：继续让 `position` 通过旧仓 admission 字段猜测新仓桥接语义。

## 2. 原因

- `position` 首版最小字段组已经固定在当前 `alpha_signal` 已正式输出字段上。
- 旧 admission 口径被明确列为不回引项。

## 3. 影响

- 阶段四后续 `position` 实施必须基于这份桥接规格推进。
