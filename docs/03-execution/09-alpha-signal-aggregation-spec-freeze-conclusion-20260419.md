# 阶段三批次 09 alpha_signal 汇总规格冻结结论

结论编号：`09`
日期：`2026-04-19`
状态：`已接受`

## 1. 裁决

- 接受：`alpha_signal` 汇总规格冻结成立。
- 拒绝：让聚合规则在代码里隐式生长的做法。

## 2. 原因

- 字段、状态和追溯口径已正式冻结。
- `alpha_signal` 与五个 trigger 库的边界已经明确。

## 3. 影响

- 阶段三实现必须把 `alpha_signal` 作为唯一正式输出。
- `position` 后续只能面向 `alpha_signal` 规划接口。
