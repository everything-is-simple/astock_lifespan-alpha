# 阶段四批次 17 portfolio_plan 最小桥接规格冻结结论

结论编号：`17`
日期：`2026-04-19`
状态：`已接受`

## 1. 裁决

- 接受：阶段四 `position -> portfolio_plan` 的最小桥接规格已经冻结。
- 拒绝：让 `portfolio_plan` 继续直接依赖 `alpha` 内部过程。

## 2. 原因

- `portfolio_plan` 最小三表已明确。
- 组合层最小裁决与 `materialization_action` 已冻结。

## 3. 影响

- 阶段四实施可以同步补齐 `portfolio_plan` 的最小正式入口。
