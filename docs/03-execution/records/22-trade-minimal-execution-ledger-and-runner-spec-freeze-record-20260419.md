# 阶段五批次 22 trade 最小执行账本与 runner 规格冻结记录

记录编号：`22`
日期：`2026-04-19`

## 1. 做了什么
1. 吸收旧版 `trade` 规格与 runner 形状中的可复用部分。
2. 只冻结阶段五最小执行账本与 runner，不纳入 `carry / exit / pnl`。

## 2. 偏差项

- 旧版 `trade_runtime` 命名未直接继承，当前仓库改为阶段五最小正式 `trade` 命名。

## 3. 备注

- 价格口径分线在本批次第一次被正式写入当前仓库规格。
