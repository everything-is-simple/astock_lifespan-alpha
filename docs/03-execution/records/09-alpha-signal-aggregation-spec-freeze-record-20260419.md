# 阶段三批次 09 alpha_signal 汇总规格冻结记录

记录编号：`09`
日期：`2026-04-19`

## 1. 做了什么

1. 冻结 `alpha_signal` 的字段与状态。
2. 固定 `signal_nk` 和追溯规则。
3. 明确首版不做跨 trigger 去重。

## 2. 偏差项

- 首版汇总规则偏保守，优先保证可追溯与完整保留。

## 3. 备注

- `position` 的选择裁决将推迟到阶段四处理。
